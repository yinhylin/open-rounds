syntax = "proto3";

option go_package = "./pb";

message Move {
  string playerID = 1;
  double x = 2;
  double y = 3;
}

message ClientEvent {
  oneof event { Move move = 1; }
}

message ServerEvent {
  oneof event { Move move = 1; }
}
